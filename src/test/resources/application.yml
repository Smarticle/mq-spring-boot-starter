cetc36:
  mq:
    rocketmq:
      enable: true
      name-server-addr: localhost:9876
      publishers:
        - { beanName: publishService1, groupId: group1, sendMsgTimeoutMillis: 5000 }
      subscribers:
        - { beanName: subscriberService1, groupId: group1, maxRetryCount: 1 }